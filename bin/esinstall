#!/usr/bin/env node
const esmInstall = require('./es-install.js');

if (!process.env.DEBUG) {
  console.debug = () => {};
}

(async () => {
  await esmInstall();
})().catch(e => {
  console.error('Error:');
  console.error(e);
});
