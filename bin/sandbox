#!/usr/bin/env node
const esmInstall = require('../src/sandbox.js');

if (!process.env.DEBUG) {
  console.debug = () => {};
}

(async () => {
  await sandbox();
})().catch(e => {
  console.log(e);
});
